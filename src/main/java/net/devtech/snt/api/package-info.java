/**
 * snt provides an intentionally open api, to maximize potential use-cases, however to hide the inevitable enterprise nature of such abstraction, we
 * also provide a neat api ontop of it that's easier to use. This is the basis for that api
 */
package net.devtech.snt.api;